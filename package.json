{
  "name": "action-squawk",
  "version": "0.0.1",
  "description": "🦉 Run squawk to lint PostgreSQL migrations",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "yarn lint && ncc build src/index.js -o dist --source-map --license licenses.txt"
  },
  "keywords": [
    "github",
    "actions",
    "lint",
    "postgres",
    "squawk"
  ],
  "homepage": "https://github.com/K-Phoen/action-squawk",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/K-Phoen/action-squawk.git"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/exec": "^1.1.0",
    "@actions/github": "^5.0.0",
    "@actions/tool-cache": "^1.6.0",
    "minimatch": "^5.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.24.0",
    "eslint": "^7.4.0"
  }
}
